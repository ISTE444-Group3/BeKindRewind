const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost:3306/",
      user: "bkrAdmin@localhost",
      password: "BeKindRewindG003",
      database: "bekindrewind",
    },
    listPerPage: 10,
  };
  module.exports = config;